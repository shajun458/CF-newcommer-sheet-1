#include <stdio.h>
int main ()
{
    double n,x;
    scanf("%lf",&n);
 
    x= n-(int)n;
 
    if (x==0)
        printf("int %.lf\n",n );
 
    else
        printf("float %d %.3lf\n",(int)n,x);
    return 0;
 }
